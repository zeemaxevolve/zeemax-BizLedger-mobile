{
  "name": "zeemax-biz-ledger-mobile",
  "version": "1.0.0",
  "description": "Zeemax Biz Ledger — Android app (Capacitor wrapper around the same React app used on desktop)",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --outfile=www/app.bundle.js --loader:.js=jsx --minify --target=es2019",
    "build:dev": "esbuild src/index.jsx --bundle --outfile=www/app.bundle.js --loader:.js=jsx --sourcemap",
    "sync": "npm run build && npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/share": "^8.0.1",
    "esbuild": "^0.28.2",
    "html2canvas": "^1.4.1",
    "jsdom": "^30.0.1",
    "jspdf": "^4.2.1",
    "lucide-react": "^0.383.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-test-renderer": "^18.3.1"
  }
}
